/**
 * openjpegwasm.js — WASM codec build glue code stub for OpenJPEG / OpenJPH.
 */

function createOpenJPEGDecoder() {
  return Promise.resolve({
    decode: function(buffer) {
      return buffer;
    }
  });
}
